{
  "host_addresses_rule": [
    {
      "type": "multiple_a",
      "min_count": 2,
      "hosts": [],
      "severity": "Info",
      "message": "Multiple A records detected — verify that load balancing or CDN routing is intentional and ensure all backend nodes are patched and configured consistently."
    }
  ],

  "name_servers_rule": [
    {
      "type": "default_ns",
      "hosts": [],
      "severity": "Medium",
      "message": "Default or ISP-provided name servers in use — switch to managed or hardened DNS providers and enforce DNSSEC to reduce manipulation and interception risks."
    }
  ],

  "rules": [],
  "version_rules": [],

  "subdomain_threshold": 100,

  "defaults": {
    "severity": "Info",
    "message": "No rule matched — confirm DNS records, verify exposure is intended, and review configuration for potential misconfigurations."
  }
}
