{
  "a_rule": [
    {
      "record_type": "A",
      "pattern": "^(?:10\\.|192\\.168\\.|172\\.(?:1[6-9]|2[0-9]|3[0-1])\\.)",
      "severity": "Medium",
      "message": "A record points to a private IP — verify that internal hosts are not exposed and update DNS to prevent accidental leakage."
    }
  ],

  "mx_rule": [
    {
      "type": "same_priority",
      "severity": "Info",
      "message": "MX records share the same priority — ensure all mail servers are patched, synchronized, and configured for proper load distribution."
    },
    {
      "type": "diff_priority",
      "severity": "Info",
      "message": "MX records have different priorities — confirm failover order and ensure backup mail servers are secured and maintained."
    },
    {
      "type": "no_mx",
      "severity": "Medium",
      "message": "Domain has no MX records — configure valid mail servers if email delivery is expected and block unintended inbound traffic."
    }
  ],

  "subdomain_threshold": 100,

  "defaults": {
    "severity": "Info",
    "message": "No rule matched — verify DNS entries for correctness and ensure no unintended records are exposed."
  }
}
